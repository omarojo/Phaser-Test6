node module ‘easyimage’ requires imagemagick to be installed
that means you need to have homebrew installed in order to install imagemagick.

After you have installed homebrew.. install imagemagick
“brew install imagemagick”

then install the npm packages

end